# Dùng khi build production (npm run build)
# Đường dẫn API và Socket.IO đều là đường dẫn TƯƠNG ĐỐI (không có domain/IP),
# trình duyệt sẽ tự ghép với domain hiện tại khi gọi.
VITE_API_BASE=/video-call/api
VITE_SOCKET_PATH=/video-call/socket.io
VITE_BASE_PATH=/video-call/
